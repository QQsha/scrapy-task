1. Activate your Virualenv
2. Install all requirement packages:
	pip install -r requirements.txt
3. To run Spider use:
	scrapy runspider fara_data.py -o fara_data.json
4. Now all json data stored in "fara_data.json" file